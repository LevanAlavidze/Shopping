import org.gradle.api.initialization.resolve.RepositoriesMode

pluginManagement {
    repositories {
        google()
        jcenter()
        gradlePluginPortal()
    }
}

rootProject.name = "Shopping"
include ':app'
